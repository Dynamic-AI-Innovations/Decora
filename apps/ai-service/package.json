{
  "name": "@decora/ai-service",
  "version": "0.1.0",
  "private": true,
  "description": "Python FastAPI render pipeline. Node tooling here is only for pnpm workspace integration.",
  "scripts": {
    "dev": "uvicorn src.main:app --reload --port 8000",
    "start": "uvicorn src.main:app --host 0.0.0.0 --port 8000",
    "test": "pytest",
    "typecheck": "mypy src",
    "lint": "ruff check src"
  }
}
